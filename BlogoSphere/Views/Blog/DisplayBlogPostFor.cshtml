@model BlogoSphere.Models.BlogPostVM

@{
    ViewBag.Title = "DisplayBlogFor";
}

<h2 style="color:coral">DisplayBlogFor</h2>

<table border="0"  style="background-color:lightgray" cellpadding="15">
    <thead>
    <tr> 
        <th>Title</th>
        <th>Created</th>
        <th>Blog/Post</th>
    </tr>
    </thead>
        
        @if (Model.BlogList.Count() == 0 && Model.PostList.Count() == 0)
        {
            <tr>
                <td colspan="3" style="color:red">
                    No matching data found.
                </td>
            </tr>
        }
        else

        {
            foreach (var item in Model.BlogList)
            {
                <tr>
                    <td>
                        <a href=@Url.Action("Details", "Blog", new { id = item.Id } )>@Html.DisplayFor(modelItem => item.Title)</a>
                    </td>

                    <td>@Html.DisplayFor(modelItem => item.Created)</td>

                    <td>Blog</td>
                </tr>
            }
            foreach (var item in Model.PostList)
            {
                <tr>
                    <td>
                        <a href=@Url.Action("Index", "Post", new { Postid = item.Id } )>@Html.DisplayFor(modelItem => item.Title)</a>
                    </td>
                    <td>@Html.DisplayFor(modelItem => item.Created)</td>
                    <td>Post</td>
                </tr>
            }
        }
 
</table>